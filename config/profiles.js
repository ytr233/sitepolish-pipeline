export const profiles = {
    "validation-only": {
        label: "Validation only",
        description:
            "Run checks and supported cleanup without adding site features.",
        overlay: null,
    },
};
